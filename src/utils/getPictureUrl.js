const getPictureUrl = function thatReturnsWaldoPictureUrl(name) {
  return `https://github.com/alextalijan/wheres-waldo-api/blob/main/assets/pictures/${name}.jpg?raw=true`;
};

export default getPictureUrl;
